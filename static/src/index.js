import React from "react";
import ReactDom from "react-dom";

ReactDom.render(
  <div>
    <p>Make a request to a python script</p>
    <button id="requestBtn">Request</button>
  </div>,
  document.getElementById("root")
);
